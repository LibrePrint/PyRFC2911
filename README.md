# PyRFC2911

Python library for IPP **servers** intended for actual printers 

# Progress

A lot has to be changed to use it in an actual printer. The \_\_main__.py file is purposefully broken because the folder shouldn't be used as a CLI tool, it should be used as a library even when finished.

UNFINISHED - Contribution welcome